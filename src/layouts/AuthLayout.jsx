import React from 'react';

const AuthLayout = ({ children }) => {
  //This file is to add some customization for all auth pages for eg Login, SignUp etc
  return <div>{children}</div>;
};

export default AuthLayout;
